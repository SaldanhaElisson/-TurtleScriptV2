inicio
    var inteiro : x, y;
    var real : velocidade;
    var logico : condicao_ativa;
    var texto : mensagem;

    x = 10;
    y = 25 + x;
    velocidade = 5.5;
    condicao_ativa = verdadeiro;
    mensagem = "Olá, mundo!";

    se condicao_ativa entao
        x = x * 2;
        velocidade = velocidade / 2;
    senao
        y = y - 5;
    fim_se

    // Loop com erro intencional
    enquanto x < 100 faca
        fim_enquanto   x = x + 1; // Linha com corpo do loop após 'fim_enquanto'

fim
